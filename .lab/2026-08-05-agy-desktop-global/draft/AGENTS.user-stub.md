# AGENTS.md — global stub (Antigravity Desktop, optional)

Pointer only — full policy lives in repo after `init -Scope project`.

- Workspace with git/repo open → follow `~/.gemini/GEMINI.md` spacex-orchestrator-sx block (lock check, ask before init).
- Lock OK in repo → load `.agents/skills/orchestrator/SKILL.md` + project `.agents/rules/`.
- No repo workspace → ignore orchestrator.
