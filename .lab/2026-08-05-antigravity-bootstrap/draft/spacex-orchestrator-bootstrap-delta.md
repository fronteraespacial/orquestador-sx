# spacex-orchestrator.md — delta to merge (post-APPROVE)

Insert **after** the Install path block, **before** Hard rules:

---

## Bootstrap (read first)

1. **`.orchestrator-lock.json`** at repo root — if missing, offer project init; do not fetch pack from chat alone.
2. Lock OK → **`.agents/skills/orchestrator/SKILL.md`** is canonical algorithm.
3. Companion always-on rules: **`cj-orchestrator-bootstrap.md`**, **`cj-criollo-changelog.md`** (Antigravity: enable Always On in UI).

## En criollo (REQUIRED on human-facing close-out)

Include `## En criollo` (3–6 sentences: what changes for human/team/device — install, update, chats, models, links, friction). Not optional on implementer/verifier handoffs to human.

---
