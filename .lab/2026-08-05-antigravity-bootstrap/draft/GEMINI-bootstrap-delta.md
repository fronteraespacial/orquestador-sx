# GEMINI.md — delta to merge (post-APPROVE)

Insert **after** the compatibility header, **before** MANDATORY ORCHESTRATION:

---

## Bootstrap (Antigravity + cross-tool)

Before orchestrated work in this repo:

1. Read **`.orchestrator-lock.json`**. Missing → offer `Orchestrator init -Scope project` (agents do not init alone unless user pasted canonical install frase).
2. Lock OK → load **`.agents/skills/orchestrator/SKILL.md`**.
3. Primary workspace rules: **`.agents/rules/cj-orchestrator-bootstrap.md`** + **`.agents/rules/spacex-orchestrator.md`** + **`.agents/rules/cj-criollo-changelog.md`** — mark **Always On** in Antigravity Customizations after install.

**En criollo:** every technical close-out includes `## En criollo` (3–6 frases prácticas). See `cj-criollo-changelog` rule.

**Chat sin repo init:** Antigravity playground or empty workspace = **no orchestrator**. Open this repo after project init.

---
